/**
 * The New-ticket composer moved into `board/new-ticket/` (a focused module per
 * shell / breadcrumb / chips / footer / file-attach). This file stays as the
 * stable import point the app-shell mount (and any references) already use.
 */
export { NewTicketDialog } from "@renderer/components/board/new-ticket/new-ticket-dialog";
