---
title: Agent capability changes
description: Agent-facing CLI, tool-surface, effect, and recovery changes by the first build that can serve them.
sidebar:
  label: Agent capability changes
---

import AgentCapabilityChanges from "../../../components/AgentCapabilityChanges.astro";

This is the capability record for agents and automation. It is not a commit log
or the app's user-facing release changelog.

Run `volli help changes` to see the source revision and unique build ID of the
CLI bundle answering now, plus the running app version when the optional local
read succeeds. Release semver remains a separate promotion marker.

<AgentCapabilityChanges />
