---
title: Concepts
description: Translate tasks, agent chats, branches, and review into the terms Volli uses in its UI and documentation.
sidebar:
  label: Concepts
---

import { CardGrid, LinkCard } from "@astrojs/starlight/components";
import AgentOperatingModel from "../../../components/AgentOperatingModel.astro";

Use this page when a Volli label is unfamiliar. The table translates the common
workflow into the terms you see in the app.

| What you want to do | Volli term |
| --- | --- |
| Plan and track a unit of work | **Ticket** |
| State the task's outcome and constraints | **Ticket Body** |
| Run an agent conversation | **Chat** |
| Keep a durable record of a chat or terminal | **Session** |
| Give a task its own Git checkout | **Ticket worktree** |
| Inspect a task's branch changes | **Change Set** |

These terms have the same meaning across the app and its documentation.

## Planning

**Project.** A tracked codebase folder. Removing it from Volli does not remove
the folder from disk.

**Home.** The project's tabbed workspace. A project opens here. Its first tab is
the Board and cannot be closed. Board chats and Project Files open as tabs
beside it.

**Ticket.** A task on the board. It holds a title, status, and Ticket Body. A
ticket can provide the context for a ticket chat and its worktree.

**Ticket Body.** The task's Markdown brief and starting instruction. It is part
of the ticket record, not a repository file.

**Ticket workspace.** The working surface for one task. Its main tabs hold the
Ticket Body, chats, files, and diffs. Its rail groups repository state,
properties, and chats under Now, with separate Diffs and Files views.

**Display ID.** The project-local handle shown to people, such as `VC-12`.

## Chats

**Session.** The durable record for a chat or terminal. It remains available
after its live connection, turn, or run ends.

**Chat.** Volli's built-in agent conversation. It uses the model you choose in
Model Access and shows the agent's messages, tool steps, and questions in the
app.

**Ticket chat.** A chat for one task. It carries the ticket's Body and resolved
context, and works in the ticket's worktree.

**Board chat.** A chat for broad project work, started from Home. It works in
your main checkout and can create and edit tickets. Use it to turn a rough idea
into tasks.

**Model Access.** The accounts, subscriptions, credentials, and models that
Volli can use. In the app, this policy is under **Settings → Models**. Choose a
default model there before you start a chat. Credentials
do not enter chat history.

**Terminal companion.** A manual terminal or external TUI you open for work in
a project or worktree: Claude Code, Codex, OpenCode, or your own. Open it from
the caret beside **Chat**. Volli never opens one as a fallback when a chat
fails.

## Git work

**Ticket worktree.** An isolated Git checkout for a task. Volli creates its
branch as `volli/<DISPLAY-ID>-<slug>` and stores the checkout under
`~/.volli/worktrees/`. A worktree is disposable checkout state. Volli can
recreate it from the retained branch after cleanup.

**Main checkout.** The repository folder you added as a project.

**Change Set.** Committed and uncommitted work in a ticket worktree, measured
against its base branch.

**Ticket Files.** The files in a ticket's worktree.

**Project Files.** The files in the main checkout.

## History and control

**Session Event.** An immutable fact in a Session's local history, such as a
message, command outcome, interaction, attachment outcome, or recovery signal.

**Command.** Explicit user intent recorded before the runtime is asked to act.

**Receipt.** The recorded outcome of a Command: accepted, rejected, completed,
or unreconciled.

**Attention.** A reconstructed view that shows when a Session needs recovery or
input. It is derived from Session history rather than a source of truth.

## Agent-facing operating model

The CLI and managed Volli skill pack use the following operating model. Run
`volli help concepts` for the same facts without opening this site.

<AgentOperatingModel />

## Automations

**Automation.** A saved, named way to start work. It contains a **Trigger**,
**Instructions**, and a **Runtime**. Every Automation can be run by hand; its
Trigger describes what else may start it.

**Trigger.** What starts an Automation besides a person: **Only when I run it**,
**Ticket enters**, or **On a schedule**. A new Automation uses **Only when I
run it**. A column Trigger makes an Automation available in selected columns;
a schedule uses one of Volli's hourly, daily, weekdays, or weekly presets.

**Instructions.** The message sent as the first message of the fresh Session a
Run opens. Instructions use the chat composer's `/` templates and Skills and
`@` file references.

**Runtime.** The model policy for a Run. It can inherit the project default,
name one of the configured default tiers, or pin an exact model and reasoning
level. A Run records the resolved model and reasoning it used.

**Run.** One invocation of an Automation and the durable record of the fresh
Session it opened. Run history remains available even if the Automation is
later deleted.

**Unbound Run.** A one-time Run created by **Run once…**. It carries its own
Instructions and names no saved Automation.

**Enabled automation.** An Automation switched on for automatic triggering on
this Mac. Enablement is off until someone turns it on here. It does not prevent
a person from running the Automation by hand.

**Armed automation.** The one Automation a column fires after a ticket makes a
Deliberate move into it. A column can arm at most one, and arming is local to
the machine and not retroactive.

**Offered list.** The Automations a column presents during a Deliberate move.
A column can offer several but fires at most one automatically. The list also
has a **Move only** choice.

**Option-drag picker.** The expanded Offered list shown while **Option (⌥)**
is held during a card drag. Up to nine offered rows get digits **1**–**9**;
**0** chooses **Move only**.

See [Automations](/guides/automations/) for the authoring and running steps.

## Next steps

<CardGrid>
  <LinkCard
    title="The board"
    href="/guides/board/"
    description="See the five columns that organize your tasks."
  />
  <LinkCard
    title="Chats and worktrees"
    href="/guides/agents-and-worktrees/"
    description="Understand how agent chats and isolated checkouts work together."
  />
</CardGrid>
