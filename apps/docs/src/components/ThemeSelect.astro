---
/*
 * Volli is dark-only, so the docs drop Starlight's light/dark toggle entirely.
 * Rendering nothing is the documented way to remove a Starlight UI component.
 * The palette itself is pinned in src/styles/volli.css.
 */
---
