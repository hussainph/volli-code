# Volli Code — v1 Concept

*Founding document, July 2026 — written for the native Swift original (`../volli-swift`) and ported to this repo when the project moved to Electron (July 2026). This is the product source of truth for v1. CLAUDE.md summarizes the parts agents need constantly; this file holds the full reasoning. Every product decision carries over unchanged; the two platform decisions (1–2) were superseded by decisions 22–25 below.*

## The problem

Terminal coding agents (Claude Code, Codex, Opencode) act as both the planning and execution layer of software work. They take on tasks autonomously for long stretches — and drift out of sync with planning done in a separate tracker (Linear). Meanwhile, terminal multiplexers built for agents (cmux) organize *sessions*, not *work*: workspaces aren't tied to tasks, so revisiting a project gives you no organized view of the body of work done on it, and no way to jump from "what happened" back into "keep going."

The disconnect: **the tracker doesn't know what the agents are doing, and the terminal doesn't know what the plan is.**

## The idea

A desktop app (macOS-only v1) where the ticket *is* the terminal workspace:

- Projects (each pointing at a codebase folder) live in a Slack-style workspace sidebar.
- Each project has a kanban/list view of tickets.
- A ticket's content — title, description, attachments — is simultaneously its *specification* and the *starting prompt* for a coding agent.
- Dragging a ticket to **Doing** creates an isolated git worktree, composes the prompt, and boots the agent of your choice in a high-fidelity embedded terminal, live-accessible inside the ticket's expanded view.
- Agent lifecycle events (via harness hooks calling a bundled CLI) move the ticket across the board — **the board reflects reality** — and fire native notifications when human attention is needed.
- Every session run against a project is preserved and indexed: an auditable history of human + AI decision-making per codebase.

### Goals

1. Merge planning, organization, and execution in one place — track ticket progress and execution/branching/PRs together.
2. Build an auditable per-project history of all agent sessions and decisions.
3. Mirror the lifecycle of AI-engineered projects to CI/CD best practices (ticket → branch → PR).
4. Expose coding agents to the planning layer without a bulky MCP.
5. Massively increase parallel session throughput via neat organization (dozens of sessions across projects).
6. Make the human a powerful orchestrator: plan and execute across multiple projects concurrently, solo or with agents.

## Decision log

Every decision below was made explicitly with the owner (July 2026), with alternatives considered. Decisions 1–2 are retained for the historical record but **superseded** by 22–24 (the Electron rewrite).

| # | Decision | Alternatives rejected | Rationale |
|---|----------|----------------------|-----------|
| 1 | *(superseded by #22)* **Native Swift macOS app** (AppKit + SwiftUI) | Electron/Tauri + xterm.js; local web app + Node sidecar | Performance is a priority; owner wants to invest in Swift/macOS skills. Volli (Next.js/Convex) reuse sacrificed knowingly — different use case. cmux proves this exact stack at production scale. |
| 2 | *(superseded by #23)* **GhosttyKit** ([Lakr233/libghostty-spm](https://github.com/Lakr233/libghostty-spm)) behind a `TerminalEngine` protocol | SwiftTerm (mis-renders agent TUIs); vendoring libghostty from source day 1 | Prebuilt XCFramework tracking stable Ghostty tags. The durable lesson: **agent TUIs need real terminal emulation**, and the engine belongs behind an interface so it can be swapped. |
| 3 | **Local SQLite**, transcripts as indexed files | Convex (volli reuse, but transcripts in cloud + network required); hybrid | Local-first fits single-player v1 where projects are local folders and sessions are local processes. Multiplayer is a someday-problem; solve sync when it hurts. |
| 4 | **Worktree per ticket, per-ticket opt-out** | Always worktree; always main checkout | Parallel tickets must not clobber each other; branch/PR maps 1:1 to ticket (goal 3). Opt-out covers quick/read-only tasks. |
| 5 | **Agent-agnostic command templates + first-class adapters** for Claude Code, Codex, Opencode | Claude Code only; pure command templates with no adapters | A single-harness app defeats the purpose of a third-party app — just use the official apps. Bring-your-own-harness is the floor; the popular 95% get deep hook/resume/transcript integration. |
| 6 | **Bundled `volli` CLI** as the agent↔planning interface | MCP server (explicit non-goal); ticket-as-files (mushy, racy); local HTTP API (clunky for agents) | One line in the agent's prompt teaches it; deterministic; harness hooks call the same binary; identical across harnesses. cmux validates the pattern (their Unix-socket CLI). |
| 7 | *(primary-session concept deleted by #39; tabs/splits stand)* **Primary session + cmux-style tabs/splits per ticket** | Single terminal; free-form panes with no primary | The primary session's lifecycle drives board automation unambiguously; splits keep the cmux ergonomics the owner loves (shell next to agent, logs, a second agent). |
| 8 | **Full lifecycle automation** | Semi-auto (manual moves); opt-in per project | The magic *is* the loop: Doing → launch; hooks → Needs Review + notification; Done → push + PR offer. "Board reflects reality" is the core promise. |
| 9 | **App-lifetime sessions + resume commands** (persist layout, cwd, resume command — relaunch via `claude --resume` etc.) | PTY-owning background daemon (right long-term, heavy now); no resume at all | cmux ships exactly this and users don't notice the difference for agent workloads. Daemon is a natural v2+ when the IPC surface exists anyway via the CLI socket. |
| 10 | **Fixed semantic columns**: Backlog · Todo · Doing · Needs Review · Done | Linear-style custom columns with semantic type tags | Dramatically simpler to build and reason about; automation semantics stay predictable. Known trade-off: imposes "our" SDLC. If demand appears, evolve toward configurable layouts. Vibe Kanban validates the minimal semantic-status shape. |
| 11 | **Standalone — replaces Linear** | One-time Linear import; two-way sync (a tarpit) | The whole point is integrating planning with execution; syncing to a planner that can't see execution recreates the original disconnect. |
| 12 | **Name: Volli Code**, CLI `volli` | — | Leans into the volli lineage. |

The following were resolved in a design review (July 2026), closing the questions originally parked below:

| # | Decision | Alternatives rejected | Rationale |
|---|----------|----------------------|-----------|
| 13 | *(trigger vocabulary to be redefined by the configurable automations system — #38/#39)* **Symmetric lifecycle moves.** Primary agent stops *or blocks* (turn finished / needs permission / asked a question) → Needs Review, with a reason badge on the card; any new primary-session activity → back to Doing. | Heuristics to detect "final" stops (fuzzy timers); explicit `volli ticket move` as the only trigger (depends on agent obedience) | Harness Stop hooks fire after *every* turn, not just task completion — so embrace it: the column is a live "whose turn is it" indicator, and Needs Review always means "blocked on you". Notifications are attention-aware (suppressed while the ticket's terminal is focused). |
| 14 | **Agent commits; Done has a safety net.** The brief instructs the agent to commit its work; the app never commits behind your back. The Done flow detects a dirty worktree, shows the diff stat, and offers one-click "commit remaining changes" before push/PR. | Auto-checkpoint commits on every Stop (history noise, commits half-done states); no policy (PR step just fails) | Honest history, no surprise commits, and the failure mode (agent forgot to commit) is caught at the exact moment it matters. |
| 15 | *(copy mechanism amended by #41 — `.worktreeinclude` + built-in defaults replace app-configured globs; setup command stands)* **Worktree prep = per-project setup command + copy-globs.** On worktree creation: run an optional setup command (e.g. `pnpm install`) and copy configured gitignored globs (e.g. `.env*`) from the main checkout. Setup runs visibly in the primary terminal before the agent command. | Symlinking `node_modules` (parallel builds corrupt each other — defeats isolation); nothing (agents burn minutes on environment archaeology) | Two simple knobs cover ~all stacks; visible execution keeps it auditable. |
| 16 | **Retention: clean on PR merge + TTL; nothing is ever destroyed by automation.** Worktrees survive Done. On PR merge (watched via `gh`) — or a configurable TTL for PR-less tickets — the app prompts to archive + clean. Archived tickets leave the board into a per-project, searchable **Archive** that permanently retains the event log, transcripts, branch name, and PR link; deleting from the Archive is the only destructive act, always explicit. | Clean immediately on Done (kills resume exactly when review bounces); manual-only (disk chore) | Reopen-and-resume stays cheap while review is live; the audit trail (goal 2) is sacred. |
| 17 | **Scratch sessions: per-project Sessions surface + promote.** One keystroke opens a ticket-less session (main checkout, no worktree, no board involvement) — still recorded in the same session history. "Promote to ticket" creates a ticket, links the transcript, and optionally moves work into a worktree/branch. | Auto-creating placeholder tickets (litters the board); deferring to v2 (recreates the original disconnect, loses transcripts) | Zero friction to start exploring, no audit hole, clean path from poking-around to tracked work. |
| 18 | *(“primary” wording superseded by #39; the explicit-CLI-from-any-pane rule stands)* **Multi-agent: automation = primary only; explicit CLI = any pane.** Hook-driven lifecycle is wired only to the primary session, but any session in the ticket (secondary agents, shells) can run explicit `volli` commands: move, comment, notify. | Hard wall (secondary agents can't even notify); all agents drive lifecycle (two heartbeats → column races) | One unambiguous heartbeat for the board; deliberate acts count from anywhere. |
| 19 | **Attachments: materialize files + list paths.** On launch, attachments are copied into a gitignored ticket-scoped dir in the worktree (`.volli/attachments/`); the brief lists each path with its label and instructs the agent to read them; URLs pass through as URLs. | Per-harness native embedding (divergent adapters for marginal gain); URLs/text only (screenshots are the most common spec artifact) | One mechanism, full fidelity, identical across every harness that can read files. |
| 20 | *(“primary session” → the ticket's live sessions, per #39)* **Backward moves interrupt, never kill.** Dragging a ticket out of the active columns sends the primary session an interrupt (Esc-equivalent — stop the current turn); terminals stay alive. If a terminal was closed by the user, a session-resume interface brings it back. Derived rule: **hook events arriving on the heels of a manual move are stale — the manual move wins** (else the interrupt's own Stop hook would bounce the ticket right back). | Killing the PTY (destroys live context); blocking the move (fights the human); label-only moves (Todo with running agents — the exact lie this app exists to kill) | Board stays truthful; nothing is lost. |
| 21 | **Re-entering Doing resumes by default.** Live terminal → untouched, keep talking. Dead terminal with a resume command → relaunch it (`claude --resume`). Only a ticket with no prior session gets the fresh composed prompt; "Start fresh session" is always in the ticket menu. | Always-fresh (agent amnesia exactly when "address the review feedback" needs context); ask-every-time (modal for a 90%-obvious answer) | The most common bounce (Needs Review → Doing) keeps the agent's context. |

The following were made when the project moved to this repo (July 2026):

| # | Decision | Alternatives rejected | Rationale |
|---|----------|----------------------|-----------|
| 22 | **Electron + React + TypeScript** — this repo rewrites the native Swift app; still macOS-only v1 | Continuing native Swift/SwiftUI; Tauri | Agent-driven development in SwiftUI proved far slower than in TS — LLM throughput beats native polish for a product whose owner builds it *with* agents. Electron keeps cmux-class terminal embedding and desktop integration available. T3 Code (the closest TS sibling product) chose Electron over Tauri after evaluating every option, for the same product shape. The Swift original (`../volli-swift`) remains a working reference further along. |
| 23 | *(renderer superseded by #26; node-pty + `TerminalEngine` seam stand)* **Terminal: xterm.js in the renderer, node-pty in the main process**, behind a `TerminalEngine` interface over the preload/IPC bridge | Naive DOM/`<pre>` rendering; embedding a native terminal surface | Same lesson as #2: agent TUIs need real terminal emulation. xterm.js is VS Code's engine — proven daily against Claude Code's TUI. The interface seam keeps the swap-the-engine escape hatch open. |
| 24 | *(build tooling superseded by #25)* **electron-vite** build/dev, **electron-builder** packaging, **Zustand** renderer state, **better-sqlite3** for SQLite | Electron Forge; hand-rolled vite+tsc (the broken initial scaffold); Redux/MobX | Fastest correct dev loop with HMR across main/preload/renderer; Zustand fits the model-resident/view-lazy state pattern inherited from the Swift app; better-sqlite3 is the mature synchronous binding suited to a main-process store. |
| 25 | **Restructure into a pnpm + Vite+ (`vp`) monorepo before feature work.** The Electron desktop app becomes a single app inside the workspace; Electron main/preload bundling moves to hand-rolled Vite+ pack configs (T3 Code's approach), replacing electron-vite. electron-builder, Zustand, and better-sqlite3 from #24 stand. | Staying single-package on electron-vite until a second client forces a migration | A mobile client is a real option and the owner refuses to be locked out of it: converting a hello-world app into a monorepo is cheap now; retrofitting a monorepo under a grown app means tracking backwards. T3 Code ships this exact layout (pnpm workspace, Vite+ toolchain, thin Electron shell) at production scale. |
| 26 | **Terminal renderer: restty** (libghostty-vt WASM + WebGPU, WebGL2 fallback) as the `TerminalEngine` implementation — exercising #23's swap-the-engine seam. `ghostty-web` (Canvas2D, same ghostty WASM parser, xterm.js-compatible API) is the pre-authorized fallback behind the same interface if restty's youth bites. | xterm.js + `@xterm/addon-webgl` (mature but not ghostty); full native libghostty (no public API, Metal can't composite into Chromium — the `electron-libghostty` overlay PoC is dead); `@wterm/*` (deliberately DOM-rendered, not GPU) | Owner's experience: Ghostty's emulation quality on agent TUIs is the bar (same lesson as #2). restty is the only shipped ghostty-derived GPU pipeline for the web; verified live in the packaged app on WebGPU under Electron 43. Known costs accepted: 0.2.x API churn, node-pty needs an Electron-ABI rebuild after every install (`rebuild:pty` — pnpm strips the prebuild's spawn-helper exec bit). |
| 27 | **Ghostty config adapter** (issue #18): the terminal's appearance/input config IS the user's real Ghostty config — `theme`, `font-family`, `font-size`, `font-feature`, `scrollback-limit`, `mouse-reporting`, `macos-option-as-alt`, `config-file` includes — read and file-watched in main, mapped onto restty, live-reloaded into running terminals. Fonts resolve against installed system fonts via the Local Font Access API (main grants the `local-fonts` permission); no bundled terminal font. | An app-native terminal settings schema; bundling font bytes (Geist Mono inlined base64 — shipped briefly inside PR #17, removed in the same PR) | One config shared across every ghostty frontend the owner runs (Ghostty, cmux, Volli — native apps get this via `ghostty_config_t`, which libghostty-vt/WASM lacks, so a per-app adapter is the only path). restty natively speaks ghostty's theme format and bundles its full theme catalog. A later settings UI edits the same file instead of inventing a second source of truth. |
| 28 | **SQLite schema v1 — record identity ≠ display identity.** Tickets get opaque UUID primary keys; `"VC-12"` is a *display ID* derived from prefix + per-project ticket number (unique per project), used in the UI and branch names only | Keeping `"VC-12"` as the primary key (Swift-schema parity) | Globally unique IDs keep the future sync/cloud door open; a prefix edit or cross-project move must never rewrite identity or cascade through the event log. Cheapest now, before real data accumulates. |
| 29 | **Persistence v1 scope: event log from day one; prefs in the same DB; demo scaffold deleted.** Migration 001 ships projects, tickets, labels, `ticket_events`, and an `app_state` kv table; every mutation writes its event in the same transaction; one-time localStorage import (projects + prefs), demo board discarded | Deferring the event log to the automation milestone; JSON snapshot files for prefs; importing demo tickets as real rows | History that isn't recorded can't be backfilled — the audit trail (goal 2) starts accruing now. One durable store beats two mechanisms and fixes the dev-vs-packaged localStorage origin split. The scaffold was labeled "never SQLite input" from the start. |
| 30 | **Labels are first-class entities.** Normalized `labels` + `ticket_labels` tables; `color` nullable — the hash-derived default applies until a user picks a color, which persists and wins | `tags` as a JSON array column (Swift-schema parity) | User-picked label colors must persist, and a schema-backed ticket↔label relation enables label search/management later; normalizing after real data accumulates is the expensive path. |
| 31 | **One spacing/width/type design language (docs/DESIGN.md), July 2026.** Six-step type scale as named tokens; a canonical 720px reading measure (`<ContentColumn>`, centered, side whitespace absorbs narrow windows); a unified 24px page gutter; and a two-tier surface model — reading surfaces center on the measure, workbench surfaces (board, list, terminals) stay fluid. The main content area becomes a **framed floating card** (rounded, hairline border) on the rail-dark backdrop — this consciously amends the earlier flat flush-content chrome-band treatment. | Per-surface spacing fixes with no shared tokens (the status quo — every surface improvised, edge-to-edge slabs); forcing one column width on all surfaces including board/terminals | Cohesion must be structural, not disciplinary: surfaces compose shared primitives instead of hand-rolling containers. The centered measure and the framed card draw the eye to content (the Linear lesson) while workbench surfaces keep the width they genuinely earn. |
| 32 | **Warm-park tier: SIGSTOP idle session process trees** (issue #51). An idle session (5 min with no output/input, ~0% CPU across the tree on two consecutive sweeps, no TCP listener in the tree, not visible, not pinned "keep awake") gets its whole tree SIGSTOP'd; macOS compresses/pages the memory (measured: ~350MB claude → ~35–40MB truly resident). Wake is SIGCONT (~117ms, imperceptible) on pane visibility or any input; input typed while parked queues in the PTY and applies on wake. Never parks a working session — every guard above is mandatory; teardown always SIGCONTs before killing (stopped processes ignore SIGTERM). **No silent failure while frozen**: parking is duty-cycled — each sweep, every auto-parked tree breathes (SIGCONT for ~1s, re-frozen only if the window surfaces no output, CPU, forked child, or new listener), so pending work a frozen process can't signal (file-watch rebuilds, timers, pollers, queued socket data) runs at worst one sweep interval late instead of never; manual Park Now freezes are exempt (explicit intent stays frozen until an explicit wake). Harness-agnostic and app-side only, honoring #51's untouchable-harness constraint. Parked state is visible (badge) with manual Park Now / Keep Awake overrides. | Kill+resume as the only idle tier (T3 Code's 30-min reaper, Zed's 5-thread cap — the resume seam caps comfortable scale at ~10 sessions); chat-UI pivot rendering from DB (doesn't fix process memory, loses the terminal); relying on the macOS compressor alone (active-page working sets stay resident ~200MB/session) | Validated live 2026-07-15 (`apps/desktop/e2e/sigstop-smoke.mjs`): the warm tier makes dozens of 24×7 sessions fit a 16GB machine (~50 parked ≈ 2GB resident) with zero perceptible resume cost — the differentiator neither T3 Code nor Zed has. Cold kill+resume (#9/#45) remains the deep tier below this. |
| 33 | **Global artifacts + @file refs (July 2026, supersedes the two-tier scheme in ticket-detail-mvp #14/#16/#17).** One artifacts location: `<project>/.volli/artifacts/` — the ticket tier, promote flow, and Artifacts tab are gone. The ticket Doc is the scratch pad; only work that outlives a ticket becomes an artifact, and that is project-scoped by nature. Referencing is a plain-text `@relative/path` token in the ticket markdown (whole-repo scope, gitignore-respecting index with artifacts force-included) — zero translation when the doc becomes an agent prompt. The editor decorates resolving refs into clickable chips that open persistent `file` tabs in the ticket tab strip (worktree-aware resolution; markdown editable, code read-only, images inline). Creation lives in the @ picker. Env contract: `VOLLI_TICKET` + `VOLLI_ARTIFACTS_DIR` (main-repo absolute), `VOLLI_TICKET_DIR` deleted. Full record + contract: `docs/plans/global-artifacts.md`. | Keeping two tiers (the ticket tier held only ephemera that belongs in the doc); an app-global cross-project pool (breaks repo portability); markdown-link/wikilink syntaxes (noise in agent prompts); a dedicated artifacts/files browser page (deferred with the nav-model rethink) | A ticket-scoped artifact is almost always either scratch (dies with the ticket → belongs in the doc) or genuinely project-level (→ belongs one tier up). One dir + one reference primitive kills the hardest-to-design surface in the app and makes artifacts reachable from anywhere a doc can mention them. |
| 34 | **Agent surface.** `volli` can read and mutate planning data, signal lifecycle state, observe sessions read-only, and notify. It cannot delete archives, manage projects/settings, or drive terminals. Explicit CLI moves equal human drags; events derive a `user`, `session`, or `automation` actor. | Full app API; read-only CLI; hard recursion caps | Agents coordinate through the visible, attributed board while destructive operations remain human-only. |
| 35 | **Workspace-global display IDs.** Prefixes are unique; display IDs are the only CLI ticket handles. Context resolves explicit flag → session/ticket env → cwd, then errors rather than guessing. UUIDs stay internal. | Mandatory project flags; UUID handles; first-match guessing | One readable handle names one ticket everywhere without weakening record identity. |
| 36 | **Bundled local CLI.** A generated shim runs the bundled client through Electron. It uses one-shot NDJSON over a `0600` Unix socket, stable text/JSON and error codes, and explicit `volli app launch`; Volli PTYs receive it on PATH automatically. | Separate runtime; system Node; auto-launch; MCP | One local runtime and one server-side validation boundary keep installation and behavior predictable. |
| 37 | **Consent-gated skill pack.** Detected harnesses share one managed skill pack plus thin adapters. Installs are global, hash-guarded, non-clobbering, refreshable, and cleanly uninstallable; `custom/` remains user-owned. | Repo copies; independent per-harness packs; monolithic instructions | One dormant-outside-Volli teaching surface avoids repo churn and duplicated harness logic. |

The following were settled in the git-worktree design review (July 2026) — full spec: `docs/plans/worktree-support.md`:

| # | Decision | Alternatives rejected | Rationale |
|---|----------|----------------------|-----------|
| 38 | **Worktrees materialize at session boot; column moves have zero side effects.** The first session created in a worktree-enabled ticket runs an idempotent, single-flight `ensure` (worktree + branch → copy step → setup) before its PTY spawns; later sessions find it ready. Board moves are pure data mutations. Column-entry effects (Doing warm-up, auto-launch, symmetric lifecycle moves) are deferred to a future **user-configurable column-automations system** with its own settings surface. On ensure failure the move stands, the error is toasted + logged, and the session does **not** fall back to the main checkout — the worktree toggle is the only sanctioned path there. | Column-driven creation (the original board-semantics table); eager warm-up on Doing entry; silent main-checkout fallback | Nothing spawns on the user's disk until execution genuinely begins; one seam covers every session path (composer kickoff, `+` button, CLI); a fallback to the shared checkout reintroduces the exact clobbering disease worktrees cure. |
| 39 | **Primary sessions are deleted as a concept.** All ticket sessions are equal weight (as ticket-detail-mvp shipped); setup runs in whichever session triggered worktree creation — definitionally the first. The future automations system defines its own trigger vocabulary instead of resurrecting a blessed session. | Reintroducing a primary marker in the tab strip; ordering conventions ("first tab is special") | Cognitive overload with no real value; automation semantics belong to the automations design, not to a per-ticket flag users must manage. |
| 40 | **Worktree home: `~/.volli/worktrees/<project-dirname>-<short-id>/<DISPLAY-ID>-<slug>/`; raw git CLI only.** App-owned and outside both the repo and `userData` (space-free, human-legible paths that rhyme with the branch name; watchers in the main checkout never recurse into worktrees; cleanup never touches user folders). Execution is `execFileSync git …` behind the injectable `RunGit` seam — never libgit2/native bindings. Creation runs a proactive collision matrix (branch checked out elsewhere → friendly hard fail; stale metadata → `git worktree prune` + one retry; dir-exists / dir-missing reconciliation) on `realpath`-canonicalized paths. Base resolution is deterministic and offline: ticket `baseBranch` → project `base_branch` → detection, from local refs (remote-tracking fallback), stamped onto the ticket at creation; **no implicit fetch, ever**; an existing ticket branch is reused, never reset. | `userData` (space in "Application Support" breaks unquoted scripts); nesting under `<repo>/.volli/` (watcher recursion, cleanup inside user repos); libgit2 (clobbers uncommitted files git CLI refuses to; corruption reports); T3-style fetch-first (network hang on ticket start) | Every serious tool (T3, Vibe Kanban, Sculptor, Conductor, Claude Code) keeps worktrees app-owned; Vibe Kanban's libgit2 rationale is decisive; local-first means kickoff never waits on the network. |
| 41 | **Worktree prep: `.worktreeinclude` + built-in defaults; sentinel-gated setup in the live terminal; cwd = worktree.** Copy rules live in a repo-root `.worktreeinclude` (gitignore syntax — the de-facto standard Conductor and Claude Code independently converged on; one file drives all three tools) layered over built-in defaults (`.env*`, harness local settings such as `.claude/settings.local.json`), `!` negation wins, traversal-guarded, never overwriting. The setup command stays a per-project DB setting. Setup is typed into the session's terminal wrapped with an exit-code sentinel; the harness command is written **only on exit 0** — visible, interactive, tracked, and pane-safe at once. The PTY's cwd *is* the worktree; the composed brief opens with an explicit orientation preamble (worktree path, branch, base; main checkout is reference-only) so agents never "reorient" themselves out of it. | App-configured copy-globs (original #15 — a Volli-only setting restating a standard); `setup && agent` one-liner (outcome invisible to the app); hidden setup process (violates #15 visibility); running setup as the pane's primary process (cmux #5032: pane dies with it); main-checkout cwd + per-harness "work over there" flags (no universal flag; agent's default edits land in the shared checkout) | Modern harnesses natively resolve linked worktrees for trust/settings via the shared `.git`, and the copy defaults close the gitignored-local-config gap — the one real loss cwd-in-worktree ever had. |
| 42 | **Persist identity, compute state; one module owns worktrees.** Durable truth is only `worktree_path`/`branch`/`base_branch` (+ the event log: `worktree_changed`, and a new `worktree_failed` with stage + stderr). Runtime phase (`creating → copying → setting-up → ready \| failed`) is transient, main-process-owned, IPC-broadcast, and recomputed from disk on boot — never trusted from a row. All worktree git execution, the ensure pipeline, phase registry, and event emission live in one `src/main/worktree/` module behind a narrow interface (`ensure`/`remove`/`getState`); `getState` returns the single composed answer so no one joins DB + events + stores to learn what happened. | A persisted status enum (lies after every crash); scattering git calls across pty/ipc/commands | A status column *will* diverge from disk; single-module ownership is the maintainability floor. |
| 43 | **Startup orphan sweep: three tiers, conservative by construction.** (1) `git worktree prune` — always safe. (2) Clean orphans (no DB row, empty status, no unreachable commits) are auto-removed, branch retained — a clean worktree dir is cache, not data, so #16's no-destruction law is honored. (3) Dirty orphans are never auto-removed: surfaced once via toast and listed in Settings → Worktrees with explicit Reveal/Delete. Dirty detection errs toward dirty: untracked files, sequencer state (merge/rebase/cherry-pick/bisect), unreachable commits, `git worktree lock`, submodule drift, **or any git failure/ambiguity** all count, and removal re-verifies cleanliness immediately before deleting. TTL-based periodic sweeps stay with the retention work (#16) and will reuse these tiers. | Vibe Kanban's force-remove-unmatched (their dirs are OS-temp; ours are durable); manual-only cleanup (T3's disk chore) | Automation may delete only what git provably already holds; everything else waits for a human. |

The following was settled in the Done-flow battle-test review (July 2026) — full spec: `docs/plans/done-flow.md`:

| # | Decision | Alternatives rejected | Rationale |
|---|----------|----------------------|-----------|
| 44 | **PR flow is a button, never a gate (amends the #75 sketch; upholds #14 and #38).** Decision #14's safety net ships as visibility + manual affordances in the ticket Details rail: two-mode diff stat (working-tree and merge-base — Codex's split), a one-click "Commit remaining changes" when dirty (fixed message `chore(<ID>): commit remaining work`), and "Push & create draft PR" (targeted best-effort `git fetch` of base first — the push flow is already a network moment, so #40's offline rule governs kickoff only; typed `gh` failure taxonomy; existing PRs detected and reopened, never errored). Divergence gates on hard blockers only (sequencer state blocks commit; rejected push surfaces typed); base-moved-ahead is info. `pr_url` persists on the ticket (the #76/#16 foundation) + a `pr_opened` event. The Done-entry adaptive dialog is deferred to #79 as a *user-configurable* automation. Network verbs run behind a new injectable async runner in the worktree module — never `execFileSync`. | The Done-entry gate/dialog (most prescriptive design in the researched cohort — VK, Conductor, Codex, T3, cmux all ship PR creation as an explicit user action; a modal would also be the lone exception to #38's pure-data moves); "base moved ahead → rebase first" as a blocker (fires on nearly every PR); holding all of #75 (loses the diff foundation #76/#81/#82 need) | The safety net matters exactly when the agent forgot to commit — a visible dirty indicator + one-click commit catches that without interrupting a gesture; prescription stays opt-in until #79 makes it configurable. |
| 45 | **The Done-flow rail is one adaptive action, never a status dashboard (amends #44's UI; owner call, July 2026).** One split button whose primary label is the resolved whole next step ("Commit & create draft PR" / "Push & create draft PR" / "Push updates" / "View PR"), always present (disabled with a tooltip reason rather than appearing/vanishing), with the unbundled verbs in its chevron menu; one merge-base context line ("what would the PR contain"). Raw git status (working-tree, ahead/behind) is read only to pick labels and disabled reasons, never rendered as lines. Grounded in comparative research: T3 Code renders no ahead/behind counts anywhere and resolves one adaptive verb; Vibe Kanban's at-rest git surface is five elements with no commit button in the product (auto-commit per agent turn); Conductor is one "Create PR" button + an aggregating Checks tab; Codex ships no ahead/behind indicator at all. | The first build's three status lines in three git reference frames + four coexisting conditional buttons (a dashboard — and a footgun: side-by-side Commit and Push meant the natural click shipped a PR missing uncommitted work); VK-style invisible auto-commit (deferred to #79 as an opt-in automation — #14's honest-history default stands) | Status a user must interpret is prescription by another name; encoding it into the single correct verb keeps the flow one glance + one click, and the stacked label makes the commit-then-push ordering mistake impossible. |

## The core loop

### Board semantics

| Column | Meaning | Automated behavior |
|--------|---------|--------------------|
| **Backlog** | Captured, not committed | none |
| **Todo** | Committed, ready to run | none |
| **Doing** | Agent (or human) actively working | Column entry itself has no side effects (#38). The ticket's **first session boot** materializes the worktree: branch `volli/<ID>-<slug>` (unless opted out) → copy `.worktreeinclude` files → run the project setup command visibly in that terminal, sentinel-gated → materialize attachments → compose prompt → write the harness command. Column-entry automation (warm-up, auto-launch) and re-entry resume (#21) arrive with the configurable automations system. |
| **Needs Review** | Blocked on a human — always | Entered when a ticket agent finishes a turn, requests permission, or asks a question (card badge shows which; trigger vocabulary owned by the future automations system per #39). Any new session activity moves it back to Doing. Fires a native notification + dock badge — suppressed while the ticket's terminal is focused. |
| **Done** | Work accepted | On entry: dirty-worktree check (diff stat + one-click "commit remaining changes") → offer branch push + PR creation (`gh pr create`, PR body from ticket + session summary). Worktree survives Done; on PR merge or Done-TTL, prompt to archive + clean. |

Moves are bidirectional and manual moves always allowed; automation never fights the human. Moving a ticket backward out of the active columns *interrupts* the ticket's live sessions (Esc-equivalent) without killing terminals; hook events that arrive on the heels of a manual move are treated as stale — the manual move wins.

**Archive:** a per-project view, not a sixth column. Lifecycle automation never destroys data — archiving removes the worktree but permanently retains the ticket, event log, transcripts, branch name, and PR link. Deleting from the Archive is the only destructive act, always explicit.

### Ticket anatomy

- Identity: per-project prefix + number (e.g. `VC-12`), Linear-style.
- Content: title, rich description (markdown), attachments (files, URLs), labels, priority.
- Execution config: harness (Claude Code / Codex / Opencode / custom command template), worktree toggle, base branch.
- Live state: worktree path, branch, session statuses.
- History: every status change, session start/stop, and comment in an append-only event log; full transcripts linked.

### Prompt composition

When a ticket enters Doing, the adapter renders the harness command from a template, e.g.:

```
claude "$(volli ticket brief VC-12)"
```

where `volli ticket brief` emits the ticket's title, description, attachment paths/URLs, and one paragraph teaching the agent the `volli` CLI (how to move the ticket, comment, ask for review). Custom harnesses get `{{brief}}`/`{{ticket_id}}` template variables.

### The `volli` CLI

The local CLI exposes the planning surface described in decisions #34–37. Run `volli help` for the command contract; keep this document focused on product semantics rather than duplicating executable help.

`volli help` is the primary discovery surface and the CLI's authoritative reference — a blind agent should never need the skill pack to operate it (measured: thin help forced ~108 commands and ~62k tokens across 7 basic tasks). It is generated from the parser's own `CommandSpec`/`OptionEntry` tables (the same way `volli help exit-codes` renders from `AGENT_ERROR_CODES`), so reference and reality cannot drift: `volli help` prints a grouped compact reference, `volli help <command>` a full detail view, and every vocabulary rejection enumerates the valid tokens inline. The skill pack is therefore activation + norms only — it points at `volli help` and never restates flag syntax. Token budgets are tested contracts, not aspirations: the bare reference and each command's detail have char/token ceilings asserted in unit tests and an e2e token bench (`apps/desktop/e2e/agent-cli-token-bench.mjs`).

Harness adapters generate hook configs on session launch — e.g. Claude Code `Stop` → `volli session done`, `Notification` → `volli notify` — scoped to the worktree so the user's global hook config is untouched.

### Sessions & audit history

- Each session records: harness, command, cwd, start/end, resume command, transcript file.
- Ticket view shows the live terminal; project view shows a searchable session history ("what has been done here, by whom, when, and why").
- On app relaunch: layout restored, each interrupted session offers one-click resume.
- **Scratch sessions**: each project has a Sessions surface beside the board — one keystroke opens a ticket-less session (main checkout, no worktree, no board involvement), recorded in the same history. "Promote to ticket" links the transcript to a new ticket and optionally moves the work into a worktree/branch.

## Build state

Working in a flow, not to a fixed schedule. What exists and what's ahead, in rough dependency order (not a timeline):

- **Built**: the pnpm + Vite+ (`vp`) monorepo scaffold; a React + TS Electron window; `@volli/shared` domain core (branch/slug rules, board ops, ticket-event types — tested); the tracker UI (project sidebar, kanban board + list views, ticket CRUD, filters/sort); SQLite persistence v1 (better-sqlite3 in main — projects, tickets, labels, append-only ticket event log, app-state kv; decisions 28–30).
- **Terminal spike** *(landed on `feat/libghostty-integration`)*: restty (ghostty-derived, WebGPU) + node-pty rendering live per-workspace shells behind the `TerminalEngine` interface — de-risks the biggest unknown (terminal fidelity for agent TUIs, the reason the original went native).
- **Tracker (remaining)**: ticket detail view — title/body editing UI (the update command and its events already exist).
- **Ticket → session**: worktree creation, prompt composition, launch a session inside the ticket view — spec: `docs/plans/worktree-support.md` (decisions #38–#43).
- **The loop**: `volli` CLI + socket, hook generation, auto-moves, native notifications, resume-on-relaunch.
- **Breadth**: splits/tabs per ticket, session history/audit view, Done → push + PR, Codex + Opencode adapters, custom command templates.

## Non-goals (v1)

- Multiplayer / sync / accounts (local-first now; **Convex is the leading candidate for a future sync layer** — the owner wants to revisit once local v1 works; it does not change decision #3 today)
- MCP server (the CLI *is* the integration surface)
- Linear import or sync
- Custom board layouts (fixed semantic columns first)
- PTY-owning daemon (resume commands instead; daemon is a v2 candidate)
- Windows/Linux (macOS-only v1, reaffirmed for the Electron rewrite; Electron keeps the option cheap later)
- Data compatibility with the Swift original (fresh start; the Swift schema is documented in `docs/SWIFT-REFERENCE.md` if a one-time import is ever wanted)

## Open questions

The original parked questions (worktree hygiene, multi-agent tickets, exploratory sessions, attachment fidelity, retention) were all resolved July 2026 — see decisions 13–21 in the log above. Remaining minor items, with defaults chosen (revisit only if they hurt):

- **Base-branch drift**: no auto-rebase in v1; the card shows a "behind base by N commits" indicator and the human tells the agent to rebase when it matters.
- **Hook-config merging**: adapters write hooks into worktree-scoped harness settings by *merging* into any copied project settings, never clobbering them.
- **Return-to-Doing signal on custom harnesses**: adapters use native hooks where available (e.g. Claude Code `UserPromptSubmit`); the fallback heuristic is Enter-terminated input written to an agent session's PTY.
- **Convex backend**: whether/when a Convex layer backs sync or multiplayer — parked until local v1 proves out.

## Inspirations

- **[T3 Code](https://github.com/pingdotgg/t3code)** — Theo Browne's open-source Electron GUI over coding-agent CLIs: worktree-per-thread, xterm.js + server-side node-pty, SQLite, provider adapters, hidden-git-ref checkpoints. The closest TS/Electron sibling; also a proof of the thin-shell-over-local-server architecture our parked daemon (decision 9) would become.
- **[cmux](https://github.com/manaflow-ai/cmux)** — resume-command persistence, OSC/CLI notifications, socket scriptability, split-pane management.
- **[Vibe Kanban](https://github.com/BloopAI/vibe-kanban)** — kanban-for-agents shape: worktree-per-task, board driven by actual agent state.
- **Linear** — tracker UX gold standard.
- **[Ghostty](https://ghostty.org)** / [libghostty](https://mitchellh.com/writing/libghostty-is-coming) — the terminal-fidelity bar; its extracted VT core now powers our renderer via [restty](https://github.com/wiedymi/restty) (decision #26).
- **volli** (`../volli`) — kanban schema + agent-job state machine as design references.
- **The Swift original** (`../volli-swift`) — this product's first implementation; working tracker UI + Ghostty terminal spike (see `docs/SWIFT-REFERENCE.md`).
