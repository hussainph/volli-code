node_modules/
.astro/
.pnpm-store/
dist/
.DS_Store
*.log
.env
.env.local
.env.*.local
dist-electron/
coverage/
apps/desktop/release/
# Stale pre-#240 build output: prepare-pi-cli.mjs used to unpack the bundled pi
# CLI here on every dev/build machine, and the formatter chokes on its vendored
# CSS if it walks in. Nothing writes here any more; this keeps old checkouts sane.
apps/desktop/resources/
.vite-plus/
.vite-hooks/
*.tsbuildinfo
docs/ROADMAP.md
.claude/settings.local.json
.claude/worktrees/
.claude/scheduled_tasks.lock
vite.config.ts.timestamp-*.mjs
volli.db*
