============================================================================
volli help
============================================================================
volli — self-documenting planning CLI for coding agents.

Read
  identify [--project <p>]
  board [--project <p>]
  ticket list [--status <column>] [--priority low|medium|high] [--label <name>] [--project <p>] [--limit <n>]
  ticket show <id> [--events <n>] [--comments <n>] [--comments-only]
  ticket events <id> [--limit <n>]
  ticket brief <id>
  worktree status [<id>]
  worktree diff [<id>] [--working-tree]
  project list
  label list [--project <p>]
  model list
  cost [--ticket <id>] [--session <handle>] [--project <p>] [--all-projects] [--since <when>] [--group-by <dimension>]

Write
  ticket create --title <text> [--body <text>|--body-file <path>] [--priority low|medium|high] [--status <column>] [--label <name>]... [--project <p>] [--harness <h>] [--base <branch>] [--no-worktree] [--dry-run]
  ticket update <id> [--title <text>] [--body <text>|--body-file <path>|--append <text>|--edit <old> <new>] [--priority low|medium|high] [--add-label <name>]... [--remove-label <name>]... [--harness <h>] [--base <branch>] [--dry-run]
  ticket move <id> --to <column> [--dry-run]
  ticket comment <id> [-m <text>|--file <path>] [--dry-run]
  ticket signal <id> --kind <kind> --verdict <verdict> [--detail <text>] [--dry-run]

Session
  session list [--project <p>] [--ticket <id>]
  session peek <id> [--lines <n>]
  session done [--reason <text>] [--dry-run]
  session blocked [--reason <text>] [--dry-run]
  session link <id> [--dry-run]
  notify -m <text> [--title <text>] [--dry-run]

App
  app launch [--timeout <n>]
  prompt baseline [--ticket <id>] [--project <p>]
  doctor [--fix] [--dry-run]
  help [<command> | <topic>]

Agent Tool Surface
  session start  Start an agent chat session on a ticket.

App-only verbs
  ticket archive  Archive a ticket (its worktree is preserved).
Session Role availability: not claimed outside a resolved Session.

Context: --project flag, then VOLLI_SESSION/VOLLI_TICKET, then a registered cwd.
Add --json to any command for structured output. Add --dry-run to a documented write to preview it.
Ids: display ticket ids (VC-12); short session ids from session list.
volli help <command> for detail. Topics: concepts, changes, exit-codes, addressing, json, orchestration.

============================================================================
volli help identify
============================================================================
identify — Resolve and print the active project, ticket, session, and session environment.

Door: Agent CLI
Verb tier: read

Usage: volli identify [options]

Options:
  --project <p>  Resolve against this project instead of the context ladder.

Example: volli identify

Notes:
- The env block reports the session PATH, how it was adopted, where each measured tool (git, gh, node, npm, pnpm, yarn, bun) resolves, and whether workspace dependencies are installed — read it before probing for tools.
- env.requiredTools names what THIS project implies: git for a repository, node and the lockfile's package manager for a JS workspace. A `-` tool not listed there is one nothing here runs, not a fault.
- env.provenance is the boot adoption; env.interactiveProvenance is the later pass that picks up what your shell's interactive startup files export (nvm, bun, rbenv, pyenv, mise). `pending` there means that pass has not landed yet.

============================================================================
volli help board
============================================================================
board — Show a project's board grouped by column.

Door: Agent CLI
Verb tier: read

Usage: volli board [options]

Options:
  --project <p>  Target project.

Example: volli board --project VC

============================================================================
volli help ticket list
============================================================================
ticket list — List a project's tickets, optionally filtered.

Door: Agent CLI
Verb tier: read

Usage: volli ticket list [options]

Options:
  --status <column>           Filter by column. (valid: backlog, todo, doing, needs-review|review, done)
  --priority low|medium|high  Filter by priority.
  --label <name>              Filter by label.
  --project <p>               Target project.
  --limit <n>                 Cap the number of rows.

Example: volli ticket list --status doing --priority high

============================================================================
volli help ticket show
============================================================================
ticket show — Show one ticket with recent events and comments.

Door: Agent CLI
Verb tier: read

Usage: volli ticket show <id> [options]

Options:
  --events <n>     How many recent events to include; 0 for none.
  --comments <n>   How many recent comments to include; 0 for none.
  --comments-only  Show comments/signals with a compact ticket header; read no event history.

Example: volli ticket show VC-12 --comments-only

Notes:
- Latest signal per kind is always printed: signals carry state, comments carry prose.
- Either count takes 0 and performs no history query; zeroing both returns a compact ticket header for signal polling.

============================================================================
volli help ticket events
============================================================================
ticket events — Print a ticket's event log.

Door: Agent CLI
Verb tier: read

Usage: volli ticket events <id> [options]

Options:
  --limit <n>  Cap the number of events.

Example: volli ticket events VC-12 --limit 20

============================================================================
volli help ticket brief
============================================================================
ticket brief — Print the agent kickoff prompt for a ticket.

Door: Agent CLI
Verb tier: read

Usage: volli ticket brief <id>

Example: volli ticket brief VC-12

============================================================================
volli help worktree status
============================================================================
worktree status — Show a ticket's worktree branch, base, and sync state.

Door: Agent CLI
Verb tier: read

Usage: volli worktree status [<id>]

Example: volli worktree status VC-12

Notes:
- Read-only; defaults to the ticket owning the current directory.

============================================================================
volli help worktree diff
============================================================================
worktree diff — Summarize a ticket's diff (the PR range by default).

Door: Agent CLI
Verb tier: read

Usage: volli worktree diff [<id>] [options]

Options:
  --working-tree  Diff the uncommitted working tree instead of the PR range.

Example: volli worktree diff VC-12 --working-tree

Notes:
- Read-only; defaults to the ticket owning the current directory.
- Default range is the merge-base diff (what the PR would contain).
- --working-tree switches to the uncommitted working-tree view.

============================================================================
volli help project list
============================================================================
project list — List all registered projects.

Door: Agent CLI
Verb tier: read

Usage: volli project list

Example: volli project list

============================================================================
volli help label list
============================================================================
label list — List a project's labels.

Door: Agent CLI
Verb tier: read

Usage: volli label list [options]

Options:
  --project <p>  Target project.

Example: volli label list --project VC

============================================================================
volli help model list
============================================================================
model list — List available providers, model ids, and reasoning levels.

Door: Agent CLI
Verb tier: read

Usage: volli model list

Example: volli model list

Notes:
- Copy a printed <provider/model> verbatim into session start --model.
- Shows only models this profile can run.

============================================================================
volli help cost
============================================================================
cost — Report what Sessions consumed: tokens, cost, and cache class.

Door: Agent CLI
Verb tier: read

Usage: volli cost [options]

Options:
  --ticket <id>           Only this ticket's spend.
  --session <handle>      Only this session's spend, by short id.
  --project <p>           Target project.
  --all-projects          Every project this profile holds, not just one.
  --since <when>          Only operations at or after this instant or look-back.
  --group-by <dimension>  Break the total down along one dimension. (valid: ticket, session, model, day)

Example: volli cost --ticket VC-12 --group-by session

Notes:
- Volli's own measurement of its Sessions, not a provider account balance or an invoice.
- ~ marks a catalogue estimate, + marks a total only partly priced, and — means nothing could be priced.
- Token classes do not overlap: cache reads bill near 0.1x and cache writes near 1.25-2x, so a falling cached share is what a rising bill starts as.
- Cost is recorded per operation, never per token class — no split of the money by class is derivable.
- --since takes an RFC 3339 instant or a look-back like 7d, 24h or 90m.
- coverage says partial when the window reaches back past the point this profile began metering.

============================================================================
volli help ticket create
============================================================================
ticket create — Create a ticket (defaults to Backlog).

Door: Agent CLI
Verb tier: coordination

Usage: volli ticket create --title <text> [options]

Options:
  --title <text>              Ticket title.
  --body <text>               Body text.
  --body-file <path>          Body from a file.
  --priority low|medium|high  Priority.
  --status <column>           Initial column. (valid: backlog, todo, doing, needs-review|review, done)
  --label <name>              Add label (repeatable).
  --project <p>               Project (name/prefix/path).
  --harness <h>               Harness id. (valid: claude-code, codex, cursor, opencode, or a registered, trusted harness)
  --base <branch>             Base branch.
  --no-worktree               Skip worktree isolation.
  --dry-run                   Validate and preview without side effects.

Effects:
- Durable write: Create one Ticket and its attributed Ticket creation event.
- Human sees: The new Ticket appears on the board in the selected column.
- Does not: Creating in Doing does not start a Session or submit a kickoff turn.
- Does not: Worktree intent is recorded, but this command does not materialize a checkout.

Example: volli ticket create --title "Fix auth" --label bug

Notes:
- Defaults to Backlog unless --status is set.
- --body and --body-file are mutually exclusive.

============================================================================
volli help ticket update
============================================================================
ticket update — Update a ticket's fields or body.

Door: Agent CLI
Verb tier: coordination

Usage: volli ticket update <id> [options]

Options:
  --title <text>              Replace the title.
  --body <text>               Replace the body.
  --body-file <path>          Replace body from a file.
  --append <text>             Append to the body.
  --edit <old> <new>          Replace one <old> with <new>.
  --priority low|medium|high  Set priority.
  --add-label <name>          Add label (repeatable).
  --remove-label <name>       Remove label (repeatable).
  --harness <h>               Set the harness. (valid: claude-code, codex, cursor, opencode, or a registered, trusted harness)
  --base <branch>             Set the base branch.
  --dry-run                   Validate and preview without side effects.

Effects:
- Durable write: Update the requested Ticket fields and append attributed Ticket events for changes.
- Human sees: Updated fields appear on the Ticket card and in its Ticket workspace.
- Does not: The Ticket does not move, no Session starts, and no worktree is materialized.

Example: volli ticket update VC-12 --edit "old" "new"

Notes:
- At most one body mutation per call.
- --edit needs exactly one match for <old>.

============================================================================
volli help ticket move
============================================================================
ticket move — Move a ticket to another column.

Door: Agent CLI
Verb tier: coordination

Usage: volli ticket move <id> --to <column> [options]

Options:
  --to <column>  Destination column. (valid: backlog, todo, doing, needs-review|review, done)
  --dry-run      Validate and preview without side effects.

Effects:
- Durable write: Update the Ticket's board status and order and append one status-change Ticket event.
- Human sees: The Ticket moves to the selected board column.
- Does not: The move does not start a Session, submit a kickoff turn, or create a worktree.

Example: volli ticket move VC-12 --to needs-review

Notes:
- Moving to the current column is a no-op.

============================================================================
volli help ticket comment
============================================================================
ticket comment — Add a comment to a ticket.

Door: Agent CLI
Verb tier: coordination

Usage: volli ticket comment <id> [options]

Options:
  -m <text>      Comment text.
  --file <path>  Read the comment from a file.
  --dry-run      Validate and preview without side effects.

Effects:
- Durable write: Create one attributed Ticket comment and its Ticket activity event.
- Human sees: The comment appears in the Ticket activity feed.
- Does not: The Ticket does not move and no Session starts.

Example: volli ticket comment VC-12 -m "Ready for review"

Notes:
- Exactly one of -m or --file.

============================================================================
volli help ticket signal
============================================================================
ticket signal — Record a typed verdict on a ticket: which stage, and how it went.

Door: Agent CLI
Verb tier: coordination

Usage: volli ticket signal <id> --kind <kind> --verdict <verdict> [options]

Options:
  --kind <kind>        Which stage this verdict is about. (valid: validate, implement, review, merge, human-gate, budget)
  --verdict <verdict>  How that stage went. (valid: pass, fail, blocked)
  --detail <text>      One line of prose for a reader; the verdict is what machines read.
  --dry-run            Validate and preview without side effects.

Effects:
- Durable write: Create one attributed Ticket signal and its signaled Ticket event, in one transaction.
- Human sees: The verdict appears in the Ticket activity feed and in ticket show's latest-signal lines.
- Does not: The Ticket does not move: signals are orthogonal to the board by design.
- Does not: No Session starts, no notification fires, and no earlier signal is edited or erased.

Example: volli ticket signal VC-12 --kind review --verdict pass --detail "Two nits, fixed"

Notes:
- Acts as this session; needs a Volli session, because a verdict is only worth what its signer is.
- Signals carry state and comments carry prose — post both when a verdict needs an argument.
- The board does not move. Use ticket move for that, deliberately.
- Append-only: a later signal of the same kind supersedes an earlier one by being newer.

============================================================================
volli help ticket archive
============================================================================
ticket archive — Archive a ticket (its worktree is preserved).

Door: app only (no agent door)
Verb tier: none

The app is the only door. Neither the Agent CLI nor the Agent Tool Surface runs this verb.

Effects:
- Durable write: Mark the Ticket archived and append its attributed archive event.
- Human sees: The Ticket leaves the active board and remains available as archived history.
- Does not: The Ticket worktree is preserved and active Sessions are not ended.

============================================================================
volli help session start
============================================================================
session start — Start an agent chat session on a ticket.

Door: Agent Tool Surface (named tool; not shell-executable)
Verb tier: control
Role availability: not claimed outside a resolved Session.

Tool: session_start

Input:
  ticket  The display id of the Ticket to work, for example VC-12.
  message (optional)  The kickoff instruction the new Session opens with. Omit to send Volli's default kickoff.
  title (optional)  A permanent title for the Session. Omit to let Volli name it from the kickoff.
  model (optional)  Run the Session on a specific model instead of the configured default.
  model.providerId  Provider id, as `model list` prints it.
  model.modelId  Model id, as `model list` prints it.
  reasoning (optional)  Reasoning level override; the chosen model must support it. (valid: off, minimal, low, medium, high, xhigh, max)

Effects:
- Durable write: Create one durable Ticket Session, freeze its start inputs, and submit its kickoff turn after a ready attachment.
- Human sees: The app raises an actionable in-app toast with Open session for an agent-originated start.
- Does not: The Ticket does not move.
- Does not: The app does not steal focus or navigate until the person uses Open session.

Notes:
- Runs in the app: attended-only, never headless; the board does not move.
- Submits a kickoff turn; a supplied message replaces the default kickoff text and names the Session.
- An explicit title is permanent; a model or reasoning override replaces the app default for this Session alone.

============================================================================
volli help session list
============================================================================
session list — List a project's active terminal and chat sessions.

Door: Agent CLI
Verb tier: read

Usage: volli session list [options]

Options:
  --project <p>  Filter by project.
  --ticket <id>  Filter by ticket.

Example: volli session list --ticket VC-12

Notes:
- Prints each session's title and short id; session peek takes either type.

============================================================================
volli help session peek
============================================================================
session peek — Peek at what a session is doing: terminal output, or a chat's tail.

Door: Agent CLI
Verb tier: read

Usage: volli session peek <id> [options]

Options:
  --lines <n>  How much trailing output to show.

Example: volli session peek a1b2c3 --lines 60

Notes:
- Handle is a short session id from session list — terminal or chat.
- A chat answers activity, last-event age, turn depth, then its transcript tail.
- --lines is trailing terminal lines (60), or chat messages (12).
- Keep peeks narrow — output consumes the caller's context.

============================================================================
volli help session done
============================================================================
session done — Record that this session's work is finished.

Door: Agent CLI
Verb tier: coordination

Usage: volli session done [options]

Options:
  --reason <text>  Human-readable reason.
  --dry-run        Validate and preview without side effects.

Effects:
- Durable write: Append a completed done signal command and receipt to the current Session ledger.
- Human sees: The done signal and optional reason appear in the Session's durable history.
- Does not: No Ticket moves, the Session identity remains openable, and its worktree is not removed.

Example: volli session done --reason "Tests pass"

Notes:
- Acts on VOLLI_SESSION; needs a Volli session.
- Records the signal in the session ledger; the board does not move. Use ticket move for that.

============================================================================
volli help session blocked
============================================================================
session blocked — Signal the current session is blocked and needs a person.

Door: Agent CLI
Verb tier: coordination

Usage: volli session blocked [options]

Options:
  --reason <text>  Human-readable reason.
  --dry-run        Validate and preview without side effects.

Effects:
- Durable write: Append a completed blocked signal command and receipt to the current Session ledger.
- Human sees: The Session raises attention in the app and shows the optional reason.
- Does not: No Ticket moves, and the Session is not archived or deleted.

Example: volli session blocked --reason "Needs credentials"

Notes:
- Acts on VOLLI_SESSION; needs a Volli session.
- Raises attention on this session; --reason is the text a person sees.

============================================================================
volli help session link
============================================================================
session link — Record the harness's own session id on the current Volli session.

Door: Agent CLI
Verb tier: coordination

Usage: volli session link <id> [options]

Options:
  --dry-run  Validate and preview without side effects.

Effects:
- Durable write: Record the harness-native resume identity on the current terminal attachment.
- Human sees: Reopening or resuming the Session uses the recorded harness conversation.
- Does not: No model turn starts, no Ticket moves, and no new Session is created.

Example: volli session link 4f1c9a2e-8b7d-4e5a-9c3f-2a1b0d6e5f4c

Notes:
- Acts on VOLLI_SESSION; needs a Volli session.
- Seeds resume-on-re-entry; usually run from the harness's session-start hook.

============================================================================
volli help notify
============================================================================
notify — Send a native notification to the user.

Door: Agent CLI
Verb tier: coordination

Usage: volli notify -m <text> [options]

Options:
  -m <text>       Notification body.
  --title <text>  Notification title.
  --dry-run       Validate and preview without side effects.

Effects:
- Durable writes: none.
- Human sees: Electron raises a native macOS notification with the supplied title and body.
- Does not: It is not an in-app Sonner toast and creates no Ticket row, Ticket event, or Session event.

Example: volli notify -m "Needs input"

============================================================================
volli help app launch
============================================================================
app launch — Launch the Volli app if it isn't already running.

Door: Agent CLI
Verb tier: read

Usage: volli app launch [options]

Options:
  --timeout <n>  Seconds to wait for readiness.

Effects:
- Durable writes: none.
- Human sees: The Volli desktop app opens or remains running.
- Does not: No Ticket or Session is created, moved, or signalled.

Example: volli app launch

Notes:
- Retry the failed command once the app is up.

============================================================================
volli help prompt baseline
============================================================================
prompt baseline — Measure the prompt baseline a fresh chat Session starts with, per section.

Door: Agent CLI
Verb tier: read

Usage: volli prompt baseline [options]

Options:
  --ticket <id>  Price a Ticket Session for this ticket instead of a project chat.
  --project <p>  Resolve against this project instead of the context ladder.

Example: volli prompt baseline

Notes:
- Token counts are estimates at 4 characters/token; the provider's own meter is the count of record.
- Each section names a cache class — how often its bytes are bought again, claimed rather than measured; message-side sections are marked, and never invalidate the Cache Prefix.
- Excludes tool definitions, the user's first message, and provider overhead, which ride on top of everything counted here.
- --ticket prices a Ticket Session instead, including that ticket's Brief.

============================================================================
volli help doctor
============================================================================
doctor — Audit the harness integration and report what it is actually doing.

Door: Agent CLI
Verb tier: read

Usage: volli doctor [options]

Options:
  --fix      Regenerate, re-run Session PATH adoption, then re-check.
  --dry-run  Validate and preview --fix without side effects.

Effects (--fix):
- Durable write: Regenerate Volli-managed CLI links, wrappers, harness configuration, and shell integration, then refresh Session PATH adoption for future Sessions.
- Human sees: The CLI prints the repair result and a fresh integration check.
- Does not: The running Session keeps its startup environment; no Ticket, Session, model turn, or notification is created.

Example: volli doctor --fix

Notes:
- Reports outcomes, not configuration: whether typing a harness's name here really reaches Volli's wrapper.
- Run it inside a Volli terminal — several checks describe the shell it runs in.
- --fix regenerates the wrappers, harness configs and shell integration, then re-runs both Session PATH adoption passes. It names the outcome and added directories for new Sessions; a Session already running keeps its startup environment.
- --dry-run is valid only with --fix and previews the repair without inspecting or touching managed files.

============================================================================
volli help help
============================================================================
help — Show this reference, a command's help, or a topic.

Door: Agent CLI
Verb tier: read

Usage: volli help [<command> | <topic>]

Example: volli help ticket create

Notes:
- Topics: concepts, changes, exit-codes, addressing, json, orchestration.

============================================================================
volli help ticket
============================================================================
ticket subcommands:
  ticket list     List a project's tickets, optionally filtered. [Agent CLI]
  ticket show     Show one ticket with recent events and comments. [Agent CLI]
  ticket events   Print a ticket's event log. [Agent CLI]
  ticket brief    Print the agent kickoff prompt for a ticket. [Agent CLI]
  ticket create   Create a ticket (defaults to Backlog). [Agent CLI]
  ticket update   Update a ticket's fields or body. [Agent CLI]
  ticket move     Move a ticket to another column. [Agent CLI]
  ticket comment  Add a comment to a ticket. [Agent CLI]
  ticket signal   Record a typed verdict on a ticket: which stage, and how it went. [Agent CLI]
  ticket archive  Archive a ticket (its worktree is preserved). [app only (no agent door)]
Run volli help ticket <subcommand> for detail.

============================================================================
volli help worktree
============================================================================
worktree subcommands:
  worktree status  Show a ticket's worktree branch, base, and sync state. [Agent CLI]
  worktree diff    Summarize a ticket's diff (the PR range by default). [Agent CLI]
Run volli help worktree <subcommand> for detail.

============================================================================
volli help project
============================================================================
project subcommands:
  project list  List all registered projects. [Agent CLI]
Run volli help project <subcommand> for detail.

============================================================================
volli help label
============================================================================
label subcommands:
  label list  List a project's labels. [Agent CLI]
Run volli help label <subcommand> for detail.

============================================================================
volli help model
============================================================================
model subcommands:
  model list  List available providers, model ids, and reasoning levels. [Agent CLI]
Run volli help model <subcommand> for detail.

============================================================================
volli help session
============================================================================
session subcommands:
  session start    Start an agent chat session on a ticket. [Agent Tool Surface (named tool; not shell-executable)]
  session list     List a project's active terminal and chat sessions. [Agent CLI]
  session peek     Peek at what a session is doing: terminal output, or a chat's tail. [Agent CLI]
  session done     Record that this session's work is finished. [Agent CLI]
  session blocked  Signal the current session is blocked and needs a person. [Agent CLI]
  session link     Record the harness's own session id on the current Volli session. [Agent CLI]
Run volli help session <subcommand> for detail.

============================================================================
volli help app
============================================================================
app subcommands:
  app launch  Launch the Volli app if it isn't already running. [Agent CLI]
Run volli help app <subcommand> for detail.

============================================================================
volli help prompt
============================================================================
prompt subcommands:
  prompt baseline  Measure the prompt baseline a fresh chat Session starts with, per section. [Agent CLI]
Run volli help prompt <subcommand> for detail.

============================================================================
volli help concepts
============================================================================
Volli operating model

Session identity and attachments
A Session is the durable identity and locally ordered history of one agentic conversation. Volli creates the Session before an executor attaches. The Session outlives a terminal pane, a structured-chat runtime, a model turn, and a restarted attachment. Reconnect and recovery can replace the live attachment without creating a different Session.
Structured chat and terminal are venues or attachments, not separate Session types. A Session Role states the product scope: project, ticket, or subagent. Role controls context, the frozen Agent Tool Surface, and future authority policy; it is not the name of the UI or process attached to the Session.

Chats, kickoff doors, and first turns
A plain chat created in the app is durable immediately and attaches in the background. It sends no model turn until a person submits the first message. A kickoff door creates or resolves the Session and submits an opening turn immediately. The opening turn is the difference; neither door creates a second kind of Session.
The app action Create & start is a composed human action. It creates a Ticket in Doing and starts a Session with a kickoff turn. The individual verbs stay independent so automation cannot acquire hidden consequences.
- ticket.move changes the board and starts no Session.
- ticket.create --status doing creates a Ticket in Doing and starts no Session.
- session.start starts a Session and does not move or create a Ticket.
- session.done and session.blocked record Session signals and do not move the board.

Tickets, worktrees, and Project Sessions
A worktree-scoped Ticket records the intent to use isolation before a checkout exists. Volli materializes or reuses the Ticket worktree when work starts. Ending a Session does not remove the checkout. Archiving a Ticket preserves its worktree; later retention or an explicit app action can remove disposable checkout state while the retained branch remains the source for recreation.
A Project Session runs on the Main checkout, the project folder the person added to Volli. It has no Ticket worktree and no board effect. A Ticket configured without worktree isolation also runs on the Main checkout, but it remains a Ticket Session because Role and work location answer different questions.

Board state, Session signals, and human effects
Ticket state and Session state are separate ledgers. A Ticket comment appears in the Ticket activity feed. A Ticket move changes the board. A Session signal appears on that Session and can raise attention. Each write documents where a person sees the result and the important actions it does not imply.
notify calls Electron's native Notification API. It raises a native macOS notification, not an in-app Sonner toast. A Session start received through the agent path raises an actionable in-app toast with Open session. It does not steal focus or navigate until the person uses that action.

Actors and the socket boundary
An Actor is user, session, automation, or unauthenticated. The app derives attribution from the door a mutation arrived through; callers never declare themselves. Ticket events keep that attribution and may cite the originating Session.
The local socket authenticates a Session by a per-attachment token, exported into that Session's environment beside VOLLI_SESSION. A caller with no valid token is the unauthenticated Actor: never the user, and never the Session it names. Its default posture is reads only, and what it may write beyond that is per-project policy a person sets in the app.
Scope that claim honestly. The token defeats an injected string and cross-session confusion; it does not defeat a hostile process running as the signed-in macOS user, which can read the token out of any environment it can see. That is precisely why the control tier is absent from the socket rather than gated on the token: a named tool call is bound to the attachment that made it and never crosses a socket, so there is no credential to steal.

The three product surfaces
The Agent CLI is the shell-composable discovery and coordination surface. It carries reads and low-risk writes that are visible, attributable, and reversible. The Agent Tool Surface is the ordered set of named tools frozen into one Session's Role bundle. Tool availability is enforcement: a tool-only verb is absent from shell execution because the Role was or was not handed that capability, not because the operation does not exist. The app is the attended human surface for navigation, decisions, curation, and visible outcomes.
Every agent-facing operation has one Verb Registry key everywhere, and that key is its identity: help, authority, a Role bundle, and a Session's durable frozen tool list all spell it. Only the surface spelling changes. For example, session.start renders as volli session start in shell syntax, and reaches a model as the named tool session_start, because no model provider accepts a dot in a tool name. The two are one verb with one handler, not two capabilities. Help names the right door. A wrong-door refusal redirects to that door; a no-door refusal means this build declares no such verb.

Derived Verb Tiers
A Verb Tier is derived from a registry entry's access modes and actor requirement. It is not a risk label stored beside the verb. Read tier means Agent CLI access for any local caller. Coordination tier means an Agent CLI write attributed to a Session actor. Control tier means a Role-gated named tool that is absent from the agent socket. A verb on no agent surface has no tier.
Inside a resolved Session, help reports the Role and the exact frozen Agent Tool Surface the app recorded. If the app is stopped, the Session is stale, or this build cannot supply that read, static help still works and says availability is unknown. It never rebuilds bundle membership from current Settings or prompt prose.

Attended runtime
Volli's structured Agent Runtime is attended. The desktop app owns its local model loop, durable Session writes, notifications, and actionable UI. Closing or failing to start that runtime does not create a headless fallback. A command that requires it returns APP_UNREACHABLE; the safe recovery is to launch the app and retry once.
Web Access is optional and may be absent from a Session's frozen tools. The managed Volli skill pack carries this operating model locally, so an agent never needs web access to discover how Volli works. The bundled volli help output remains the live authority if a managed file was user-edited and became stale.

============================================================================
volli help changes
============================================================================
Volli Agent CLI capability changes

Bundle identity
  CLI package: @volli/cli 0.0.1
  Release promotion marker: 0.1.0
  Source revision: 8e8a17c0+VC-91
  Build id: reference-fixture
  Running app: not reached (static help remains available)
  Capability baseline: 8e8a17c0

VC-178 (after VC-163)
Added
- ticket signal --dry-run. Every voluntary coordination write now previews through the shared contract, and the append-only one is where a rehearsal matters most.
Changed
- ticket show and ticket events print formatted signal, event and comment rows in text mode instead of raw JSON, so the cheap poll returns lines rather than blobs.
- Comment bodies, signal details and free-text event payloads are bounded per field and hoisted into one untrusted-prose envelope per response, cited from their row by a [n] token. Quoting keeps identical polls byte-identical; ticket_await keeps its per-wake nonce because one wake is delivered once.
Fixed
- volli doctor decides Session liveness from the attachment token the socket door verified rather than from the live-PTY map, so an environment that can write is no longer reported as ended.
Removed
- None in this record.

VC-163 (after VC-85)
Added
- Per-attachment session tokens. Volli mints one when it spawns a Session's terminal or attachment and exports it as VOLLI_SESSION_TOKEN; the socket verifies it. A Session now authenticates itself instead of announcing itself.
- The unauthenticated Actor, for a socket caller Volli cannot identify. It may read the board and, by default, write nothing — a posture a person can widen per project, per verb.
- FORBIDDEN_ACTOR (exit class 1), the refusal for a caller the project's policy does not let run a verb. Distinct from WRONG_DOOR, which is about the surface rather than the caller.
Changed
- No environment variable is read as the user any more. An absent or forged VOLLI_SESSION resolves to the unauthenticated Actor rather than to the highest-trust Actor in the system.
- Coordination-tier verbs — ticket create/update/move/comment, notify, session done/blocked/link/harness, hook — require an authenticated Session unless a project grants them more widely.
- ticket.comment stamps its comment row from the authenticated Actor rather than from raw VOLLI_SESSION, so a comment can no longer cite a Session that did not write it.
- Command detail no longer prints shell usage, argv options, or a copyable example for a verb the shell does not execute. A tool-only verb shows its callable name and input fields instead; an app-only verb names the app.
Fixed
- None in this record.
Removed
- session.start left the Agent CLI. It is control tier now: the named session_start tool in the project Role's bundle is the agent path, and the app is the human one. Typing it in a shell answers WRONG_DOOR and starts nothing.
- ticket.archive left every agent surface. Archiving is app-only curation; no Role bundle carries it and no CLI access mode projects it. Help still names it, so a wrong door stays distinguishable from no door.

VC-85 (after VC-162)
Added
- ticket signal, an authenticated Session's typed verdict channel for the stage and outcome of ticket work.
- ticket_await, the named Agent Tool Surface tool that parks a Session until a ticket signals, is commented on, or moves.
Changed
- ticket show treats --events 0 and --comments 0 as none, so a cheap poll returns latest signals without either history log.
Fixed
- ticket_await waits are lossless: its opaque cursor replays a matching event committed between calls or while no wait was parked.
Removed
- None in this record.

VC-162 (after VC-91)
Added
- A Role-scoped Agent Tool Surface: the named tools a Session holds are now resolved from its Role at creation, not handed identically to every Session.
- session_start as a named tool in the project Role's bundle. A Project Session can start a Ticket Session without touching the agent socket; the calling Session and its project are bound by Volli from the attachment, so the tool takes a ticket and nothing about the caller.
- A SESSION TOOLS block in every Session's first message, naming the Volli verbs that Session holds under their callable names, and stating that what is absent will not become available mid-Session.
Changed
- session.start gained the named session_start tool on the Agent Tool Surface beside its Agent CLI door. One registry key, one handler, two doors — its verb tier stayed coordination while the CLI door remained open. VC-163 then closed that door; see the entry above.
- A Session's frozen tool surface can now name Verb Registry keys alongside coding and interaction tools. The durable record spells the dot-key; only the provider wire uses the underscored form.
- A Ticket Session's tool array contains no agent-control tool, and cannot acquire one while it runs. Availability is decided once, when the Session is created.
Fixed
- None in this record.
Removed
- None in this record.

VC-91 (after 8e8a17c0)
Added
- volli help concepts and volli help changes as app-independent local topics.
- Structured human-visible effects and explicit non-effects in command detail.
- --dry-run previews for ticket create/update/move/comment, Session done/blocked/link, notify, and doctor --fix. The shared dryRun tool contract is ready for session.start when VC-162 supplies its tool seam.
- A preview is refused up front unless the running app declares the preview contract on a context-free identify probe, so --dry-run can never execute as a real write on an older app build, and never inherits a Project or Session refusal the verb itself did not need.
- An app-side refusal when a verb declares no preview at all, so a dryRun argument built outside the bundled CLI cannot be ignored on the way to a real write.
- Structured JSON error reason and next fields while stable error codes remain unchanged.
Changed
- Help discovers all declared Verb Registry doors and can report a resolved Session's frozen Agent Tool Surface.
- Managed Volli skill files and the docs site project the same concepts, effects, recovery, and capability data.
Fixed
- Unknown help paths now fail with valid commands and topics instead of silently returning bare help.
- A declared tool-only verb reached through the shell is a wrong-door refusal, distinct from an undeclared no-door name.
Removed
- None in this record.

============================================================================
volli help exit-codes
============================================================================
Exit codes: 0 ok; 1 failure; 2 usage; 3 app unreachable (retryable).

Error codes:
  USAGE                2 usage
    Why: The typed arguments do not match this verb's declared syntax.
    Next: Run `volli help <command>` and retry with the documented arguments.
  INVALID_REQUEST      2 usage
    Why: The request reached its verb but one value or value combination is invalid.
    Next: Read `volli help <command>`, correct the named value, and retry.
  UNSUPPORTED_COMMAND  2 usage
    Why: This build declares no verb matching the typed name.
    Next: Run `volli help` and choose one of the commands or topics it lists.
  WRONG_DOOR           2 usage
    Why: The verb exists, but the shell is not the surface that executes it.
    Next: Use the surface named in the refusal; if Role availability is unknown, inspect the Session Runtime Brief or `volli help <verb>` first.
  FORBIDDEN_ACTOR      1 failure
    Why: The verb is on this surface, and this caller is not one the project's policy lets run it. Coordination-tier verbs want an authenticated Volli Session; a process that merely runs as your user is not one.
    Next: Run the command from inside a Volli Session. If you already are, the project's per-actor policy withholds this verb and only a person can widen it in Settings — do not work around the refusal.
  APP_UNREACHABLE      3 app unreachable (retryable)
    Why: The local desktop host or one of its required runtimes could not answer.
    Next: Run `volli app launch`, wait for readiness, then retry the same command once.
  DB_UNAVAILABLE       1 failure
    Why: The app could not open the durable local database.
    Next: Open the Volli app and inspect its database error before retrying the command.
  PROJECT_REQUIRED     1 failure
    Why: The verb needs a Project, but the context ladder resolved none.
    Next: Pass `--project <name|prefix|path>` or run from a registered Project directory.
  PROJECT_NOT_FOUND    1 failure
    Why: The selected Project no longer exists or does not match a registered Project.
    Next: Run `volli project list`, then retry with a printed Project name, prefix, or path.
  AMBIGUOUS_PROJECT    1 failure
    Why: More than one registered Project matches the selector.
    Next: Retry with the exact Project path printed in the refusal.
  TICKET_NOT_FOUND     1 failure
    Why: No live Ticket matches the supplied Display ID in the resolved Project context.
    Next: Run `volli ticket list` in the intended Project and retry with a printed Display ID.
  AMBIGUOUS_TICKET     1 failure
    Why: The Display ID matches Tickets in more than one Project.
    Next: Resolve the intended Project explicitly, then retry the Ticket verb.
  SESSION_NOT_FOUND    1 failure
    Why: The supplied or attributed Session identity is stale, missing, or unresolved.
    Next: Run `volli session list` and use a current printed handle; for current-Session verbs, start from a live Volli Session.
  AMBIGUOUS_CONTEXT    1 failure
    Why: The context ladder found more than one equally valid target.
    Next: Pass the exact Project or Ticket selector requested by the refusal.
  CONTEXT_REQUIRED     1 failure
    Why: The verb needs Project, Ticket, or Session context that no safe fallback can infer.
    Next: Run `volli identify`, then supply the missing explicit selector or use a live Volli Session.
  CONTEXT_MISMATCH     1 failure
    Why: Explicit context conflicts with the Session or directory context Volli resolved.
    Next: Run `volli identify` and retry from the intended Session or with matching explicit context.
  BODY_MATCH_FAILED    1 failure
    Why: An exact Ticket Body edit found zero or multiple matches, so applying it could clobber newer prose.
    Next: Read the fresh Ticket Body, choose text that appears exactly once, and retry the edit.
  INVALID_COLUMN       1 failure
    Why: The supplied board column is outside the accepted column vocabulary.
    Next: Use a column named in the refusal or in `volli help ticket move`.
  INVALID_PRIORITY     1 failure
    Why: The supplied priority is outside the accepted priority vocabulary.
    Next: Use `low`, `medium`, or `high` and retry.
  ARCHIVED_TICKET      1 failure
    Why: The target Ticket is archived, so ordinary Ticket mutations are closed.
    Next: Open the Ticket in the app and restore it before retrying, or choose a live Ticket.
  SESSION_ENDED        1 failure
    Why: The target Session attachment has ended and cannot accept this live mutation.
    Next: Open or start a live Session, then repeat the action against that Session.
  MODEL_REQUIRED       1 failure
    Why: No runnable default model or explicit model was available for Session start.
    Next: Configure a default in Model Access or pass a model printed by `volli model list`.
  MODEL_UNAVAILABLE    1 failure
    Why: The requested model, provider sign-in, or reasoning level is not runnable now.
    Next: Run `volli model list`, then retry with an available printed model and reasoning level.
  PREFIX_CONFLICT      1 failure
    Why: A Project ticket prefix conflicts with another registered Project.
    Next: Resolve the duplicate prefix in Project Settings before retrying.
  FILE_READ_FAILED     1 failure
    Why: A CLI file argument could not be read safely before the request was sent.
    Next: Check the named path and permissions, or pass the content inline, then retry.
  MUTATION_FAILED      1 failure
    Why: The operation failed without enough durable outcome evidence for an automatic retry.
    Next: No safe retry is known; inspect current durable state first.
  SOCKET_PROTOCOL      1 failure
    Why: The CLI connected to the app but the versioned request or response exchange was invalid.
    Next: Run `volli help changes` and confirm the CLI bundle and running app come from a compatible build before retrying.
  TIMEOUT              1 failure
    Why: The app accepted the connection but did not produce an outcome within the command's bound.
    Next: Inspect the current Ticket or Session state before deciding whether one retry is safe.

============================================================================
volli help addressing
============================================================================
Context ladder: explicit --project flag, then VOLLI_SESSION/VOLLI_TICKET, then a registered cwd. Volli never guesses; ambiguity is an error. VOLLI_SESSION attributes the current socket caller in this build; it does not authenticate that process.

============================================================================
volli help json
============================================================================
Pass --json to any command for stable structured JSON output. Failures keep a stable code and add reason plus next; next is null when Volli cannot name a safe action. Dry-run plans use one versioned object shape on CLI and tool doors.

============================================================================
volli help orchestration
============================================================================
Read before writing; work your own board unless instructed; do not drive another Session's terminal or chain-spawn work.
Signals carry state and comments carry prose: ticket signal --kind <stage> --verdict pass|fail|blocked is the machine-readable verdict, ticket comment is the argument for it. Never spell a verdict as a first line of prose.
A signal never moves the board. A deliberate ticket move is what changes a column, and it stays a separate act.
ticket show prints the latest signal per kind; --comments-only and --events 0 keep a poll cheap.
No CLI verb waits. Nothing here blocks, and sleeping in a shell to poll is how sessions wedge — waiting is a named tool the runtime suspends the turn for, never a command.

============================================================================
volli help session harness
============================================================================
session subcommands:
  session start    Start an agent chat session on a ticket. [Agent Tool Surface (named tool; not shell-executable)]
  session list     List a project's active terminal and chat sessions. [Agent CLI]
  session peek     Peek at what a session is doing: terminal output, or a chat's tail. [Agent CLI]
  session done     Record that this session's work is finished. [Agent CLI]
  session blocked  Signal the current session is blocked and needs a person. [Agent CLI]
  session link     Record the harness's own session id on the current Volli session. [Agent CLI]
Run volli help session <subcommand> for detail.

============================================================================
volli help hook
============================================================================
USAGE Unknown help path "hook" (commands: identify, board, ticket list, ticket show, ticket events, ticket brief, worktree status, worktree diff, project list, label list, model list, cost, ticket create, ticket update, ticket move, ticket comment, ticket signal, ticket archive, session start, session list, session peek, session done, session blocked, session link, notify, app launch, prompt baseline, doctor, help; topics: concepts, changes, exit-codes, addressing, json, orchestration). Next: Run `volli help`, then ask for one listed command or topic.

============================================================================
volli help nonsense
============================================================================
USAGE Unknown help path "nonsense" (commands: identify, board, ticket list, ticket show, ticket events, ticket brief, worktree status, worktree diff, project list, label list, model list, cost, ticket create, ticket update, ticket move, ticket comment, ticket signal, ticket archive, session start, session list, session peek, session done, session blocked, session link, notify, app launch, prompt baseline, doctor, help; topics: concepts, changes, exit-codes, addressing, json, orchestration). Next: Run `volli help`, then ask for one listed command or topic.

============================================================================
volli help exit-codes extra
============================================================================
USAGE Unknown help path "exit-codes extra" (commands: identify, board, ticket list, ticket show, ticket events, ticket brief, worktree status, worktree diff, project list, label list, model list, cost, ticket create, ticket update, ticket move, ticket comment, ticket signal, ticket archive, session start, session list, session peek, session done, session blocked, session link, notify, app launch, prompt baseline, doctor, help; topics: concepts, changes, exit-codes, addressing, json, orchestration). Next: Run `volli help`, then ask for one listed command or topic.

============================================================================
volli help ticket create VC-1
============================================================================
ticket create — Create a ticket (defaults to Backlog).

Door: Agent CLI
Verb tier: coordination

Usage: volli ticket create --title <text> [options]

Options:
  --title <text>              Ticket title.
  --body <text>               Body text.
  --body-file <path>          Body from a file.
  --priority low|medium|high  Priority.
  --status <column>           Initial column. (valid: backlog, todo, doing, needs-review|review, done)
  --label <name>              Add label (repeatable).
  --project <p>               Project (name/prefix/path).
  --harness <h>               Harness id. (valid: claude-code, codex, cursor, opencode, or a registered, trusted harness)
  --base <branch>             Base branch.
  --no-worktree               Skip worktree isolation.
  --dry-run                   Validate and preview without side effects.

Effects:
- Durable write: Create one Ticket and its attributed Ticket creation event.
- Human sees: The new Ticket appears on the board in the selected column.
- Does not: Creating in Doing does not start a Session or submit a kickoff turn.
- Does not: Worktree intent is recorded, but this command does not materialize a checkout.

Example: volli ticket create --title "Fix auth" --label bug

Notes:
- Defaults to Backlog unless --status is set.
- --body and --body-file are mutually exclusive.

============================================================================
volli help ticket create
============================================================================
ticket create — Create a ticket (defaults to Backlog).

Door: Agent CLI
Verb tier: coordination

Usage: volli ticket create --title <text> [options]

Options:
  --title <text>              Ticket title.
  --body <text>               Body text.
  --body-file <path>          Body from a file.
  --priority low|medium|high  Priority.
  --status <column>           Initial column. (valid: backlog, todo, doing, needs-review|review, done)
  --label <name>              Add label (repeatable).
  --project <p>               Project (name/prefix/path).
  --harness <h>               Harness id. (valid: claude-code, codex, cursor, opencode, or a registered, trusted harness)
  --base <branch>             Base branch.
  --no-worktree               Skip worktree isolation.
  --dry-run                   Validate and preview without side effects.

Effects:
- Durable write: Create one Ticket and its attributed Ticket creation event.
- Human sees: The new Ticket appears on the board in the selected column.
- Does not: Creating in Doing does not start a Session or submit a kickoff turn.
- Does not: Worktree intent is recorded, but this command does not materialize a checkout.

Example: volli ticket create --title "Fix auth" --label bug

Notes:
- Defaults to Backlog unless --status is set.
- --body and --body-file are mutually exclusive.

============================================================================
volli frobnicate
============================================================================
UNSUPPORTED_COMMAND No Volli verb matches "frobnicate" (declared verbs: identify, board, ticket list, ticket show, ticket events, ticket create, ticket update, ticket move, ticket comment, ticket signal, ticket archive, ticket brief, worktree status, worktree diff, project list, label list, model list, cost, session list, session peek, session start, session done, session blocked, session link, notify, doctor, prompt baseline, app launch, help; topics: concepts, changes, exit-codes, addressing, json, orchestration)

============================================================================
volli identify --not-an-option
============================================================================
USAGE Unknown option --not-an-option (options: --project) — see volli help identify

============================================================================
volli board --not-an-option
============================================================================
USAGE Unknown option --not-an-option (options: --project) — see volli help board

============================================================================
volli ticket list --not-an-option
============================================================================
USAGE Unknown option --not-an-option (options: --status, --priority, --label, --project, --limit) — see volli help ticket list

============================================================================
volli ticket show ORACLE-1 --not-an-option
============================================================================
USAGE Unknown option --not-an-option (options: --events, --comments, --comments-only) — see volli help ticket show

============================================================================
volli ticket events ORACLE-1 --not-an-option
============================================================================
USAGE Unknown option --not-an-option (options: --limit) — see volli help ticket events

============================================================================
volli ticket brief ORACLE-1 --not-an-option
============================================================================
USAGE Unknown option --not-an-option — see volli help ticket brief

============================================================================
volli worktree status ORACLE-1 --not-an-option
============================================================================
USAGE Unknown option --not-an-option — see volli help worktree status

============================================================================
volli worktree diff ORACLE-1 --not-an-option
============================================================================
USAGE Unknown option --not-an-option (options: --working-tree) — see volli help worktree diff

============================================================================
volli project list --not-an-option
============================================================================
USAGE Unknown option --not-an-option — see volli help project list

============================================================================
volli label list --not-an-option
============================================================================
USAGE Unknown option --not-an-option (options: --project) — see volli help label list

============================================================================
volli model list --not-an-option
============================================================================
USAGE Unknown option --not-an-option — see volli help model list

============================================================================
volli cost --not-an-option
============================================================================
USAGE Unknown option --not-an-option (options: --ticket, --session, --project, --all-projects, --since, --group-by) — see volli help cost

============================================================================
volli ticket create --not-an-option
============================================================================
USAGE Unknown option --not-an-option (options: --title, --body, --body-file, --priority, --status, --label, --project, --harness, --base, --no-worktree, --dry-run) — see volli help ticket create

============================================================================
volli ticket update ORACLE-1 --not-an-option
============================================================================
USAGE Unknown option --not-an-option (options: --title, --body, --body-file, --append, --edit, --priority, --add-label, --remove-label, --harness, --base, --dry-run) — see volli help ticket update

============================================================================
volli ticket move ORACLE-1 --not-an-option
============================================================================
USAGE Unknown option --not-an-option (options: --to, --dry-run) — see volli help ticket move

============================================================================
volli ticket comment ORACLE-1 --not-an-option
============================================================================
USAGE Unknown option --not-an-option (options: -m, --message, --file, --dry-run) — see volli help ticket comment

============================================================================
volli ticket signal ORACLE-1 --not-an-option
============================================================================
USAGE Unknown option --not-an-option (options: --kind, --verdict, --detail, --dry-run) — see volli help ticket signal

============================================================================
volli ticket archive ORACLE-1 --not-an-option
============================================================================
WRONG_DOOR volli ticket archive exists in the app only; no agent surface executes it.

============================================================================
volli session start ORACLE-1 --not-an-option
============================================================================
WRONG_DOOR volli session start exists on the Agent Tool Surface as session.start; the Agent CLI does not execute it.

============================================================================
volli session list --not-an-option
============================================================================
USAGE Unknown option --not-an-option (options: --project, --ticket) — see volli help session list

============================================================================
volli session peek ORACLE-1 --not-an-option
============================================================================
USAGE Unknown option --not-an-option (options: --lines) — see volli help session peek

============================================================================
volli session done --not-an-option
============================================================================
USAGE Unknown option --not-an-option (options: --reason, --dry-run) — see volli help session done

============================================================================
volli session blocked --not-an-option
============================================================================
USAGE Unknown option --not-an-option (options: --reason, --dry-run) — see volli help session blocked

============================================================================
volli session link ORACLE-1 --not-an-option
============================================================================
USAGE Unknown option --not-an-option (options: --dry-run) — see volli help session link

============================================================================
volli notify --not-an-option
============================================================================
USAGE Unknown option --not-an-option (options: -m, --message, --title, --dry-run) — see volli help notify

============================================================================
volli app launch --not-an-option
============================================================================
USAGE Unknown option --not-an-option (options: --timeout) — see volli help app launch

============================================================================
volli prompt baseline --not-an-option
============================================================================
USAGE Unknown option --not-an-option (options: --ticket, --project) — see volli help prompt baseline

============================================================================
volli doctor --not-an-option
============================================================================
USAGE Unknown option --not-an-option (options: --fix, --dry-run) — see volli help doctor

============================================================================
volli help --not-an-option
============================================================================
ok {"command":"help","args":{"path":["--not-an-option"]},"json":false}

============================================================================
volli session harness ORACLE-1 --not-an-option
============================================================================
USAGE Unknown option --not-an-option (options: --mint) — see volli help session harness
